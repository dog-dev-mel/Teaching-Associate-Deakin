<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ConferenceSessions.ViewModel"
             x:Class="ConferenceSessions.View.SessionsPage">
    <ContentPage.Content>
        <StackLayout>
                <StackLayout.BindingContext>
                    <local:SessionsViewModel />
                </StackLayout.BindingContext>
                <ListView x:Name="myList" ItemsSource="{Binding SessionList}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding SessionTitle}" Detail="{Binding SessionDescription}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
    </ContentPage.Content>
</ContentPage>
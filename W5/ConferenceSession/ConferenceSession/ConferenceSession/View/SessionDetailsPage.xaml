<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ConferenceSession.View"
             x:Class="ConferenceSession.View.SessionDetailsPage"
             Title="week5">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
                <StackLayout Grid.Row="0"
                             Grid.Column="1">
                    <Label  Text="{Binding Session.SessionDescription}"></Label>
                </StackLayout>
                <StackLayout Grid.Row="0"
                             Grid.Column="1">
            <Label  Text="{Binding Session.SessionSpeaker}"></Label>
                </StackLayout>
                <StackLayout Grid.Row="0"
                             Grid.Column="1">
            <Label  Text="{Binding Session.SessionTitle}"></Label>
                </StackLayout>
                <StackLayout Grid.Row="0"
                             Grid.Column="1">
            <Label  Text="{Binding Session.IsTechnical}"></Label>
                </StackLayout>
    </Grid>
</ContentPage>